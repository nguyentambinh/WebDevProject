<div class="sb-wrap">
    <div class="sb-logo"></div>

    <div class="sb-menu">
        <a href="@Url.Action("Index","FNManager")" class="sb-item active" title="Dashboard"></a>
        <a href="@Url.Action("Income","FNManager")" class="sb-item" title="Thu từ dự án"></a>
        <a href="@Url.Action("Expense","FNManager")" class="sb-item" title="Chi phí"></a>
        <a href="@Url.Action("Profit","FNManager")" class="sb-item" title="Thống kê lợi nhuận"></a>
    </div>

    <div class="sb-bottom">
        <button type="button" class="sb-item" title="Settings"></button>
        <button type="button" class="sb-item" title="Logout"></button>
    </div>
</div>

<style>
    .sb-wrap {
        height: 100%;
        background: var(--card);
        color: var(--text);
        border-right: 1px solid #111827;
        padding: 20px 0;
        box-sizing: border-box;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .sb-logo {
        width: 40px;
        height: 40px;
        border-radius: 14px;
        background: linear-gradient(135deg, #38bdf8, #4f46e5);
        margin-bottom: 32px;
    }

    .sb-menu {
        display: flex;
        flex-direction: column;
        gap: 16px;
    }

    .sb-item {
        width: 40px;
        height: 40px;
        border-radius: 14px;
        border: none;
        background: #111827;
        cursor: pointer;
        transition: 0.2s;
    }

        .sb-item.active {
            background: #38bdf8;
        }

        .sb-item:hover:not(.active) {
            background: #1f2937;
        }

    .sb-bottom {
        margin-top: auto;
        display: flex;
        flex-direction: column;
        gap: 16px;
    }
</style>